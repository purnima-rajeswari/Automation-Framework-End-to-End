package com.ui.test;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;

import com.ui.pages.HomePage;
import com.ui.pages.LoginPage;
import com.ui.pages.MyAccountPage;
import com.utility.BrowserUtility;

public class LoginTest {

	public static void main(String[] args) {
/*	WebDriver driver = new ChromeDriver();
		HomePage homePage= new HomePage(driver);
		LoginPage loginPage=homePage.goToLoginPage();
		MyAccountPage myAccountPage=loginPage.loginWith("papowat559@andinews.com", "password");
		String userName=myAccountPage.getUserName();
		System.out.println(userName);*/
		
		
		/*	BrowserUtility browserUtility = new BrowserUtility(driver);
		browserUtility.goToURL("http://www.automationpractice.pl/");
		By signInLocator = By.xpath("//a[contains(text(),'Sign in')]");
		browserUtility.clickOn(signInLocator);
		
		browserUtility.sendText(emailTBLocator, "purnima.rajeswari@gmail.com");
		
		browserUtility.sendText(pwdLocator, "Password");
		
		browserUtility.clickOn(submitCredButton);*/
	}

}
